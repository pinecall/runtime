---
name: Bug
about: Something the runtime, the CLI or the box does that it should not
labels: bug
---

## What happened

<!-- One paragraph. What you did, what you expected, what came back. -->

## How to see it again

<!-- The commands, in order, from a checkout or a box. Say which: `pinecall-runtime doctor`'s
first line is the environment it read. A call's log (`pinecall sessions show <call>`) with the
keys removed is the best evidence there is. -->

## Where

- runtime version (`pinecall-runtime --version` or the commit):
- ring: laptop (`docker compose`) · box (`PINECALL_ROLE=`):
- the door or the verb:

## Anything else

<!-- A log line, a traceback, a screenshot of the console. Never a key: compare by sha256. -->
