## What changes, and why

<!-- The problem, then the change. The commit bodies say why; this says it once for the whole
diff. A rename or a removal: name the old symbol, so a reader can grep. -->

## The docs that describe it

<!-- CLAUDE.md: a change lands with its page. Tick what you touched, or say why nothing needed it.
- [ ] ARCHITECTURE.md (a module, an entity, the import table, the path a call takes)
- [ ] README.md · .env.example (a verb, a flag, a deploy step, a variable)
- [ ] docs/protocol/ (a public contract)
- [ ] .claude/skills/ (a procedure with a trap in it)
- [ ] CHANGELOG.md, one line under Unreleased
-->

## How it was checked

<!-- `scripts/format`, `scripts/lint` and `scripts/test` exit 0, and the ring you ran beyond
them: Postgres, a model, a voice, a box (`make deploy` ends with the doctor). Paste the doctor's
report if a box was involved — it prints names and never a key. -->
