# pinecall-evals

The rings a Pinecall agent is held to: goldens, judges, simulated callers, the score: `pinecall.evals`.

One of the distributions the Pinecall runtime is released as, at one version; `pip install
pinecall` installs them all. What it is, and why: agents/docs/testing-an-agent.md.
