# pinecall-db

The Pinecall runtime's database: the driver's one door, the pool, the numbered migrations: `pinecall.db`.

One of the distributions the Pinecall runtime is released as, at one version; `pip install
pinecall` installs them all. What it is, and why: docs/a-box-in-production.md.
