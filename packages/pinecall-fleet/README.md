# pinecall-fleet

The workers: the job a spoken call runs in, their heartbeat, the loop over the clouds: `pinecall.fleet`, `pinecall.worker`.

One of the distributions the Pinecall runtime is released as, at one version; `pip install
pinecall` installs them all. What it is, and why: docs/scaling.md.
