# pinecall-settings

Every variable the Pinecall runtime reads, once, and the one function that reads them: `pinecall.settings`.

One of the distributions the Pinecall runtime is released as, at one version; `pip install
pinecall` installs them all. What it is, and why: docs/the-environment.md.
