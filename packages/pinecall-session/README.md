# pinecall-session

One Pinecall call running, written or spoken: the prompt, the turns, the tools, the judge: `pinecall.session`.

One of the distributions the Pinecall runtime is released as, at one version; `pip install
pinecall` installs them all. What it is, and why: ARCHITECTURE.md §6.
