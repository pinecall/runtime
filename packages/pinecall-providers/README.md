# pinecall-providers

The vendors a Pinecall call runs on: the catalogue, the models, the voices, the prices: `pinecall.providers`.

One of the distributions the Pinecall runtime is released as, at one version; `pip install
pinecall` installs them all. What it is, and why: ARCHITECTURE.md §1.
