"""The one place the version lives; hatch reads it from here at build time."""

# The maintainer's number: a release bumps its last digit (a `v*` tag), and no code ever writes it.
__version__ = "0.1.0"
