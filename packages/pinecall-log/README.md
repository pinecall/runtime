# pinecall-log

The call log: every entry of a call, folded into its state, kept in Postgres or in memory: `pinecall.log`.

One of the distributions the Pinecall runtime is released as, at one version; `pip install
pinecall` installs them all. What it is, and why: ARCHITECTURE.md §8, docs/protocol/the-line.md.
